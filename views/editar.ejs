<!DOCTYPE html>
<html lang="pt-br">

    <%-include('./components/head')%>


<body>
    <div class="container">
        <span><% if(message != ""){ message } %></span>
        <form method="POST" action="/editar/<%= tarefa.id %>">
            <div class="item">
                <input type="text" placeholder="Digite uma nova tarefa" name="nome" value="<%= tarefa.nome %>" required>
            </div>
</body>
</html>